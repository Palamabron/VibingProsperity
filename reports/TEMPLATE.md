# Experiment report — YYYY-MM-DD HH:MM (local)

## Metadata

- **Round / days:** (e.g. round 0, days -2 and -1)
- **Git commit (optional):** `git rev-parse --short HEAD`

## Raw: data analytics (`uv run data_analytics.py`)

```text
(paste output here)
```

## Raw: backtest (`uv run backtest.py --merge-pnl --no-progress`)

```text
(paste output here)
```

## Metrics (extracted)

- **Total profit (all days in run):**
- **Per day / per product (if needed):**

## Insights

- What the numbers and/or code change imply about market behaviour or strategy.

## Hypotheses for next iteration

- Concrete, testable ideas (e.g. adjust EMA alpha, widen inner quotes on TOMATOES, tighten taker threshold).

## Algorithm changes (summary)

- Bullet list of what changed in `algorithm.py` vs the previous report.
